#include<iostream>
#include<string.h>

using namespace std;

int main()
{
    /*
    char ch = ' ';
    int Size = ch;

    cout<<Size;*/

/*
    char str1[50],str2[50],str3[50];

    strcpy(str1,"12Hello12 World23");
    strcpy(str2,"12345");
    strcpy(str3,"33256.3265");

    int a = atoi(str1);
    int b = atoi(str2);
    int c = atoi(str3);

    cout<<a<<endl; // 0 because "Hello World" is a string
    cout<<b<<endl; // 12345 is a num stored in string var which is then converted to int
    cout<<c<<endl; //33256 i.e only num before decimal is printed

    */
    /*
    char str[]="Sajag.Majhi";

    char *ptr;

    ptr = strchr(str,'.'); //search for '.' in str

    cout<<*ptr<<endl;   // prints .
    cout<<ptr<<endl;    // prints .Majhi
    */

    /*
    char str[]="hello";
    char str2[]="123.352";
    char str3[]="456";

    float k1=atof(str);
    float k2=atof(str2);
    float k3=atof(str3);

    cout<<k1<<endl; //0 since "hello" is purely a string
    cout<<k2<<endl; //123.352 converted float
    cout<<k3<<endl; //456 prints int as it is
*/


    return 0;
}
